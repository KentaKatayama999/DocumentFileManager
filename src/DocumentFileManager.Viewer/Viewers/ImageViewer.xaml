<UserControl x:Class="DocumentFileManager.Viewer.Viewers.ImageViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer x:Name="ScrollViewer"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
            <Grid>
                <Grid.LayoutTransform>
                    <ScaleTransform x:Name="ScaleTransform" ScaleX="1" ScaleY="1"/>
                </Grid.LayoutTransform>
                <Image x:Name="ImageControl"
                       Stretch="Uniform"
                       MouseLeftButtonDown="ImageControl_MouseLeftButtonDown"
                       MouseLeftButtonUp="ImageControl_MouseLeftButtonUp"
                       MouseMove="ImageControl_MouseMove"
                       Cursor="Hand"/>
            </Grid>
        </ScrollViewer>

        <!-- ツールバー -->
        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Center"
                    Background="#CC000000"
                    Margin="10">
            <Button Content="100%" Click="ZoomActual_Click" Margin="5" Padding="10,5"/>
            <Button Content="拡大 +" Click="ZoomIn_Click" Margin="5" Padding="10,5"/>
            <Button Content="縮小 -" Click="ZoomOut_Click" Margin="5" Padding="10,5"/>
            <Button Content="画面に合わせる" Click="ZoomFit_Click" Margin="5" Padding="10,5"/>
            <TextBlock Text="{Binding Zoom, StringFormat={}{0:P0}}"
                       VerticalAlignment="Center"
                       Foreground="White"
                       Margin="10,0"/>
        </StackPanel>
    </Grid>
</UserControl>
